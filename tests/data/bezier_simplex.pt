# Sample Bezier simplex .pt fixture
# This file is intentionally kept as a small, plain-text artifact
# to avoid Git LFS requirements for contributors and CI.
#
# Automated tests generate real .pt files at runtime (see
# `test_load_case_insensitive_pt`), so this fixture is only
# intended for lightweight documentation or manual inspection.
#
# If you need an actual binary .pt file, run the relevant tests
# or use the library's save() functionality to generate one.
