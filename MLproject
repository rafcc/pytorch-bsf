name: pytorch-bsf
conda_env: conda.yaml
entry_points:
  main:
    parameters:
      data: {type: path}
      label: {type: path}
      degree: {type: int}
    command: >
      python -m torch_bsf
      --data={data}
      --label={label}
      --degree={degree}
