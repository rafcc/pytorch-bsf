name: pytorch-bsf
conda_env: environment.yml
entry_points:
  main:
    parameters:
      data: {type: path}
      label: {type: path}
      degree: {type: int}
    command: >
      python -m torch_bsf
      --data={data}
      --label={label}
      --degree={degree}
